#include <stdio.h>

const int TOTAL = 3;
float average(int length, int scores[]);

int main(void)
{
	/*int total = get_int("Total umber of scores: "); */
	int scores[TOTAL];
	for (i = 0; i < TOTAL; i++)
		{
			scores[i] = get_int("Enter score: ");
			/*scanf("%d\n", &scores); */
		}
	printf("Average: %f\", TOTAL / (float) sum );

}
float average(int length, int scores[])
{
	int sum = 0;
	

}